// Abuse-prevention limits for group rooms. Shared by the API routes that
// enforce them and (for copy) the UI that explains them.
export const MAX_GROUP_MEMBERS = 100;
export const MAX_GROUPS_PER_USER = 1;
